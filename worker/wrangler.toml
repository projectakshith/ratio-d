name = "ratiod-proxy"
main = "index.ts"
compatibility_date = "2026-10-04"

[vars]
ALLOWED_ORIGIN = "https://getratiod.lol"

# Set these as secrets via: wrangler secret put HMAC_SECRET
# Set BACKEND_URLS as: wrangler secret put BACKEND_URLS
# Format: https://render-instance.onrender.com,https://your-tunnel.cfargotunnel.com
